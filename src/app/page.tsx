'use client';
import ThemeChange from '@/components/common/ThemeChange';
import { Book } from 'lucide-react';

const Page = () => {
    return (
        <>
            <ThemeChange />
        </>
    );
};

export default Page;

